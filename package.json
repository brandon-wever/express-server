{
  "name": "express-server",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "build": "node src/server.js",
    "build:watch": "nodemon src/server.js",
    "test": "set NODE_ENV=test && mocha --reporter spec --exit --timeout=10000 tests/",
    "test:watch": "set NODE_ENV=test && nodemon --exec \"mocha --reporter spec --exit --timeout=10000 tests\"/"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/brandon-wever/express-server.git"
  },
  "keywords": [
    "express",
    "js"
  ],
  "author": "Brandon Wever",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "moment": "^2.29.1",
    "mongoose": "^5.13.0",
    "passport": "^0.4.1",
    "passport-jwt": "^4.0.0",
    "passport-local": "^1.0.0",
    "underscore": "^1.13.1"
  },
  "devDependencies": {
    "chai": "^4.3.4",
    "mocha": "^9.0.3",
    "nodemon": "^2.0.7",
    "supertest": "^6.1.4"
  }
}
